[package]
name = "paste-vcfs"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "paste-vcfs"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
# Uses both the safe bcf API and raw rust_htslib::htslib FFI (RegionReader).
rust-htslib = "0.44"
